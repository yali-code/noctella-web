import { describe, expect, it, vi } from "vitest";
import * as uc from "../src/application/purchase";
import { auditPurchaseUseCaseSource, runPurchaseUseCaseAudit } from "../src/scripts/purchaseUseCaseAudit";
import { auditPurchaseRepositorySources } from "../src/scripts/purchaseRepositoryAudit";

const t="2026-01-01T00:00:00.000Z";
function ctx(){let n=0;const purchases=new Map<string,any>(),suppliers=new Map<string,any>(),receipts=new Map<string,any>(),inv=new Map<string,any>([["prod",{productId:"prod",quantity:3,updatedAt:t}]]),moves:any[]=[];const pr:any={create:vi.fn(i=>{if(i.erpReferenceId&&[...purchases.values()].some(p=>p.erpReferenceId===i.erpReferenceId)){const e:any=new Error("dup");e.name="DuplicatePurchaseReferenceError";throw e}const p=Object.freeze({...i,lines:Object.freeze(i.lines.map((l:any)=>Object.freeze({...l,purchaseId:i.id})))});purchases.set(i.id,p);return p}),findById:vi.fn(id=>purchases.get(id)??null),findByReference:vi.fn(ref=>[...purchases.values()].find(p=>[p.erpReferenceId,p.externalReference,p.invoiceReferenceNumber].includes(ref))??null),existsByReference:vi.fn(ref=>!![...purchases.values()].find(p=>p.erpReferenceId===ref)),list:vi.fn((q:any={})=>{const rows=[...purchases.values()].filter(p=>(!q.status||p.status===q.status)&&(!q.supplierId||p.supplierId===q.supplierId));return{rows,total:rows.length,limit:q.limit??50,offset:q.offset??0}}),update:vi.fn((id,patch)=>{const p=purchases.get(id);if(!p)return null;const np=Object.freeze({...p,...patch});purchases.set(id,np);return np}),updateWithVersion:vi.fn((id,ver,patch)=>{const p=purchases.get(id);if(!p)return{ok:false,issue:{code:"not_found"}};if(p.updatedAt!==ver)return{ok:false,issue:{code:"optimistic_conflict"}};const np=Object.freeze({...p,...patch});purchases.set(id,np);return{ok:true,value:np}}),addLine:vi.fn(l=>{const p=purchases.get(l.purchaseId);const line=Object.freeze({...l});purchases.set(p.id,Object.freeze({...p,lines:Object.freeze([...p.lines,line])}));return line}),updateLine:vi.fn((id,patch)=>{for(const p of purchases.values()){const i=p.lines.findIndex((l:any)=>l.id===id);if(i>=0){const line=Object.freeze({...p.lines[i],...patch});const lines=[...p.lines];lines[i]=line;purchases.set(p.id,Object.freeze({...p,lines:Object.freeze(lines)}));return line}}return null}),removeLine:vi.fn(id=>{for(const p of purchases.values())if(p.lines.some((l:any)=>l.id===id)){purchases.set(p.id,Object.freeze({...p,lines:Object.freeze(p.lines.filter((l:any)=>l.id!==id))}));return true}return false})};const sr:any={create:vi.fn(i=>{if(i.erpReferenceId&&[...suppliers.values()].some(s=>s.erpReferenceId===i.erpReferenceId)){const e:any=new Error("dup");e.name="DuplicateSupplierReferenceError";throw e}const s=Object.freeze({...i});suppliers.set(i.id,s);return s}),findById:vi.fn(id=>suppliers.get(id)??null),findByCode:vi.fn(),findByName:vi.fn(),list:vi.fn((q:any={})=>[...suppliers.values()].filter(s=>!q.status||s.status===q.status).sort((a,b)=>a.name.localeCompare(b.name))),existsByCode:vi.fn(),update:vi.fn((id,patch)=>{const s=suppliers.get(id);if(!s)return null;const ns=Object.freeze({...s,...patch});suppliers.set(id,ns);return ns}),updateWithVersion:vi.fn((id,ver,patch)=>{const s=suppliers.get(id);if(!s)return{ok:false,issue:{code:"not_found"}};if(s.updatedAt!==ver)return{ok:false,issue:{code:"optimistic_conflict"}};const ns=Object.freeze({...s,...patch});suppliers.set(id,ns);return{ok:true,value:ns}})};const rr:any={append:vi.fn(i=>{if(receipts.has(i.idempotencyKey)){const e:any=new Error("dup");e.name="PurchaseReceiptIdempotencyConflictError";throw e}const r=Object.freeze({...i,lines:Object.freeze(i.lines)});receipts.set(i.idempotencyKey,r);return r}),findById:vi.fn(),findByIdempotencyKey:vi.fn(k=>receipts.get(k)??null),listByPurchase:vi.fn(id=>[...receipts.values()].filter(r=>r.purchaseId===id)),listByPurchaseLine:vi.fn(),listByReference:vi.fn()};const invRepo={findByProduct:vi.fn((id:string)=>inv.get(id)??null),updateWithVersion:vi.fn((id:string,q:number,_v:string,updatedAt:string)=>{const x={productId:id,quantity:q,updatedAt};inv.set(id,x);return x})};const moveRepo={append:vi.fn((m:any)=>{moves.push(m);return m})};const repos={purchases:pr,suppliers:sr,receipts:rr};const uow={run:vi.fn(async(fn:any)=>fn({repositories:{purchaseRepositories:repos,inventoryRepositories:{inventory:invRepo,stockMovements:moveRepo}}}))};return{c:{purchaseRepositories:repos,purchaseRepository:pr,supplierRepository:sr,purchaseReceiptRepository:rr,unitOfWork:uow,clock:{now:()=>new Date(t)},idGenerator:{newId:()=>`id-${++n}`},logger:{},configuration:{purchaseApplicationContext:true}},repos,uow,purchases,suppliers,receipts,inv,moves,invRepo,moveRepo};}
async function po(x=ctx(),over:any={}){return uc.createPurchaseUseCase(x.c as any).execute({erpReferenceId:over.erpReferenceId, supplierId:over.supplierId, shippingCost:over.shippingCost, buyerPremium:over.buyerPremium, customsCost:over.customsCost, packagingCost:over.packagingCost, taxVat:over.taxVat, miscellaneousCost:over.miscellaneousCost, lines:over.lines??[{productId:"prod",titleSnapshot:"A",quantity:2,unitPurchaseCost:5}]});}

describe("Sprint 32A-P4 purchase service migration completion",()=>{
const supplierCases=[
["creates supplier through use case",async()=>{const x=ctx();expect(await uc.createSupplierUseCase(x.c as any).execute({name:" Acme ",erpReferenceId:"s1"})).toMatchObject({name:"Acme",normalizedName:"acme"});expect(x.uow.run).toHaveBeenCalledTimes(1)}],
["updates supplier through use case",async()=>{const x=ctx();const s=await uc.createSupplierUseCase(x.c as any).execute({name:"A"});expect(await uc.updateSupplierUseCase(x.c as any).execute({id:s.id,name:"B",expectedVersion:t})).toMatchObject({name:"B"})}],
["gets supplier",async()=>{const x=ctx();const s=await uc.createSupplierUseCase(x.c as any).execute({name:"A"});expect(await uc.getSupplierUseCase(x.c as any).execute({id:s.id})).toMatchObject({id:s.id})}],
["lists supplier filters",async()=>{const x=ctx();await uc.createSupplierUseCase(x.c as any).execute({name:"B",status:"Inactive"});await uc.createSupplierUseCase(x.c as any).execute({name:"A"});expect((await uc.listSuppliersUseCase(x.c as any).execute({status:"Active"})).map(s=>s.name)).toEqual(["A"])}],
["rejects duplicate supplier reference",async()=>{const x=ctx();await uc.createSupplierUseCase(x.c as any).execute({name:"A",erpReferenceId:"s"});await expect(uc.createSupplierUseCase(x.c as any).execute({name:"B",erpReferenceId:"s"})).rejects.toThrow()}],
["rejects missing supplier update",async()=>{await expect(uc.updateSupplierUseCase(ctx().c as any).execute({id:"none",name:"B"})).rejects.toThrow("Supplier not found")}],
["rejects stale supplier version",async()=>{const x=ctx();const s=await uc.createSupplierUseCase(x.c as any).execute({name:"A"});await expect(uc.updateSupplierUseCase(x.c as any).execute({id:s.id,expectedVersion:"old"})).rejects.toThrow("changed")}],
["supplier output is immutable",async()=>{const s=await uc.createSupplierUseCase(ctx().c as any).execute({name:"A"});expect(Object.isFrozen(s)).toBe(true)}],
];it.each(supplierCases)("supplier: %s",async(_n,run:any)=>run());
const purchaseCases=[
["creates purchase",async()=>{const p=await po();expect(p).toMatchObject({currency:"EUR",itemSubtotal:10,status:"Draft"})}],
["updates purchase",async()=>{const x=ctx();const p=await po(x);expect(await uc.updatePurchaseUseCase(x.c as any).execute({id:p.id,notes:"n",expectedVersion:t})).toMatchObject({notes:"n"})}],
["gets purchase",async()=>{const x=ctx();const p=await po(x);expect(await uc.getPurchaseUseCase(x.c as any).execute({id:p.id})).toMatchObject({id:p.id})}],
["lists purchase by status",async()=>{const x=ctx();await po(x);expect((await uc.listPurchasesUseCase(x.c as any).execute({status:"Draft"})).total).toBe(1)}],
["marks ordered",async()=>{const x=ctx();const p=await po(x);expect(await uc.markPurchaseOrderedUseCase(x.c as any).execute({id:p.id})).toMatchObject({status:"Ordered",orderedAt:t})}],
["cancels purchase",async()=>{const x=ctx();const p=await po(x);expect(await uc.cancelPurchaseUseCase(x.c as any).execute({id:p.id})).toMatchObject({status:"Cancelled"})}],
["keeps optional supplier",async()=>{expect((await po(ctx(),{supplierId:null})).supplierId).toBeNull()}],
["keeps optional product",async()=>{const p=await po(ctx(),{lines:[{productId:null,titleSnapshot:"Free",quantity:1,unitPurchaseCost:1}]});expect(p.lines[0].productId).toBeNull()}],
["supports free text line",async()=>{const p=await po(ctx(),{lines:[{titleSnapshot:"Text",quantity:1,unitPurchaseCost:1}]});expect(p.lines[0].titleSnapshot).toBe("Text")}],
["calculates totals",async()=>{expect((await po(ctx(),{shippingCost:2,buyerPremium:1,customsCost:1,packagingCost:1,taxVat:1,miscellaneousCost:1})).totalCost).toBe(17)}],
["purchase output is immutable",async()=>{expect(Object.isFrozen(await po())).toBe(true)}],
["rejects duplicate purchase reference",async()=>{const x=ctx();await po(x,{erpReferenceId:"p"});await expect(po(x,{erpReferenceId:"p"})).rejects.toThrow()}],
];it.each(purchaseCases)("purchase: %s",async(_n,run:any)=>run());
const receiveCases=[
["partial receipt",async()=>{const x=ctx();const p=await po(x);expect((await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]})).purchase.status).toBe("PartiallyReceived")}],
["complete receipt",async()=>{const x=ctx();const p=await po(x);expect((await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:2}]})).purchase.status).toBe("Received")}],
["receipt replay",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect((await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]})).replayed).toBe(true)}],
["receipt replay conflict",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});await expect(uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:2}]})).rejects.toThrow("conflict")}],
["rollback on invalid line",async()=>{const x=ctx();const p=await po(x);await expect(uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:"bad",quantityReceived:1}]})).rejects.toThrow();expect(x.receipts.size).toBe(0)}],
["rejects over receipt",async()=>{const x=ctx();const p=await po(x);await expect(uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:3}]})).rejects.toThrow("quantity")}],
["updates inventory",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:2}]});expect(x.inv.get("prod").quantity).toBe(5)}],
["appends stock movement",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.moves[0]).toMatchObject({type:"purchase_receipt",stockBefore:3,stockAfter:4})}],
["appends receipt",async()=>{const x=ctx();const p=await po(x);const r=await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(r.receipt.lines).toHaveLength(1)}],
["uses transaction",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.uow.run).toHaveBeenCalled()}],
["receipt output mapping",async()=>{const x=ctx();const p=await po(x);const r=await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",note:"n",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(r).toMatchObject({replayed:false,receipt:{note:"n"}})}],
];it.each(receiveCases)("receiving: %s",async(_n,run:any)=>run());
const adapterCases=[
["create supplier calls supplier repository in UOW",async()=>{const x=ctx();await uc.createSupplierUseCase(x.c as any).execute({name:"A"});expect(x.repos.suppliers.create).toHaveBeenCalled()}],
["create purchase calls purchase repository in UOW",async()=>{const x=ctx();await po(x);expect(x.repos.purchases.create).toHaveBeenCalled()}],
["receive calls receipt repository",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.repos.receipts.append).toHaveBeenCalled()}],
["receive calls inventory repository only through UOW",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.invRepo.updateWithVersion).toHaveBeenCalled()}],
["receive calls stock movement repository only through UOW",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.moveRepo.append).toHaveBeenCalled()}],
["use case audit blocks direct DB",()=>expect(auditPurchaseUseCaseSource("select * from purchases").status).toBe("FAIL")],
["use case audit blocks direct transaction",()=>expect(auditPurchaseUseCaseSource("db.transaction(() => {})").issues).toContain("manual transaction")],
["use case audit blocks direct inventory repository",()=>expect(auditPurchaseUseCaseSource("import '../repositories/inventory'").issues).toContain("direct Inventory repository")],
["use case audit blocks stock mutation",()=>expect(auditPurchaseUseCaseSource("stockQuantity = 1").issues).toContain("direct stock update")],
["use case audit blocks duplicated generic validation",()=>expect(auditPurchaseUseCaseSource("type PatchUseCase = {}").issues).toContain("generic patch")],
["repository audit blocks stock movement leakage",()=>expect(auditPurchaseRepositorySources([{path:"x.ts",content:"applyStockMovement()"}])).toHaveLength(1)],
["repository audit blocks transactions",()=>expect(auditPurchaseRepositorySources([{path:"x.ts",content:"db.transaction(() => {})"}])).toHaveLength(1)],
["purchase use case audit passes fixtures",()=>expect(runPurchaseUseCaseAudit().status).toBe("PASS")],
];it.each(adapterCases)("adapter/audit: %s",async(_n,run:any)=>run());
const legacyCases=[
["allocation remains out of use case",()=>expect(auditPurchaseUseCaseSource("AllocatePurchaseCosts").status).toBe("PASS")],
["purchase history remains out of use case",()=>expect(auditPurchaseUseCaseSource("productPurchaseHistory").status).toBe("PASS")],
["landed cost summary remains out of use case",()=>expect(auditPurchaseUseCaseSource("getPurchaseLandedCostSummary").status).toBe("PASS")],
["candidate helper remains out of use case",()=>expect(auditPurchaseUseCaseSource("findSupplierCandidates").status).toBe("PASS")],
["reference helper remains repository-shaped",async()=>{const x=ctx();const p=await po(x,{erpReferenceId:"ref"});expect(await x.repos.purchases.findByReference("ref")).toStrictEqual(p)}],
["receipt history retained",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.repos.receipts.listByPurchase(p.id)).toHaveLength(1)}],
["supplier name normalization retained",async()=>{const s=await uc.createSupplierUseCase(ctx().c as any).execute({name:"  A  B  "});expect(s.normalizedName).toBe("a b")}],
["cancel blocks received workflow",async()=>{const x=ctx();const p=await po(x);await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:2}]});await expect(uc.cancelPurchaseUseCase(x.c as any).execute({id:p.id})).rejects.toThrow("cannot be cancelled")}],
];it.each(legacyCases)("legacy retained: %s",async(_n,run:any)=>run());

const moreCases=[
["rejects blank supplier",async()=>{await expect(uc.createSupplierUseCase(ctx().c as any).execute({name:"   "})).rejects.toThrow("required")}],
["updates supplier type",async()=>{const x=ctx();const s=await uc.createSupplierUseCase(x.c as any).execute({name:"A"});expect(await uc.updateSupplierUseCase(x.c as any).execute({id:s.id,supplierType:"Dealer"})).toMatchObject({supplierType:"Dealer"})}],
["supplier default type",async()=>{expect((await uc.createSupplierUseCase(ctx().c as any).execute({name:"A"})).supplierType).toBe("Other")}],
["supplier default status",async()=>{expect((await uc.createSupplierUseCase(ctx().c as any).execute({name:"A"})).status).toBe("Active")}],
["purchase rejects missing purchase",async()=>{await expect(uc.getPurchaseUseCase(ctx().c as any).execute({id:"missing"})).rejects.toThrow("Purchase not found")}],
["purchase rejects stale update",async()=>{const x=ctx();const p=await po(x);await expect(uc.updatePurchaseUseCase(x.c as any).execute({id:p.id,expectedVersion:"old"})).rejects.toThrow("changed")}],
["purchase rejects non eur",async()=>{await expect(uc.createPurchaseUseCase(ctx().c as any).execute({currency:"USD" as any,lines:[{titleSnapshot:"A",quantity:1,unitPurchaseCost:1}]})).rejects.toThrow("EUR")}],
["purchase rejects empty lines",async()=>{await expect(uc.createPurchaseUseCase(ctx().c as any).execute({lines:[]})).rejects.toThrow("at least one")}],
["purchase rejects zero quantity",async()=>{await expect(uc.createPurchaseUseCase(ctx().c as any).execute({lines:[{titleSnapshot:"A",quantity:0,unitPurchaseCost:1}]})).rejects.toThrow("quantity")}],
["purchase rejects negative cost",async()=>{await expect(uc.createPurchaseUseCase(ctx().c as any).execute({lines:[{titleSnapshot:"A",quantity:1,unitPurchaseCost:-1}]})).rejects.toThrow("negative")}],
["ordered purchase cannot update",async()=>{const x=ctx();const p=await po(x);await uc.markPurchaseOrderedUseCase(x.c as any).execute({id:p.id});await expect(uc.updatePurchaseUseCase(x.c as any).execute({id:p.id,notes:"x"})).rejects.toThrow("current status")}],
["ordered transition rejects repeat",async()=>{const x=ctx();const p=await po(x);await uc.markPurchaseOrderedUseCase(x.c as any).execute({id:p.id});await expect(uc.markPurchaseOrderedUseCase(x.c as any).execute({id:p.id})).rejects.toThrow("transition")}],
["cancel stale rejects",async()=>{const x=ctx();const p=await po(x);await expect(uc.cancelPurchaseUseCase(x.c as any).execute({id:p.id,expectedVersion:"old"})).rejects.toThrow("changed")}],
["mark ordered stale rejects",async()=>{const x=ctx();const p=await po(x);await expect(uc.markPurchaseOrderedUseCase(x.c as any).execute({id:p.id,expectedVersion:"old"})).rejects.toThrow("changed")}],
["add line retains purchase id",async()=>{const x=ctx();const p=await po(x);expect(await uc.addPurchaseLineUseCase(x.c as any).execute({purchaseId:p.id,titleSnapshot:"B",quantity:1,unitPurchaseCost:1})).toMatchObject({purchaseId:p.id})}],
["update line maps output",async()=>{const x=ctx();const p=await po(x);expect(await uc.updatePurchaseLineUseCase(x.c as any).execute({purchaseId:p.id,lineId:p.lines[0].id,titleSnapshot:"C"})).toMatchObject({titleSnapshot:"C"})}],
["remove line maps output",async()=>{const x=ctx();const p=await po(x);expect(await uc.removePurchaseLineUseCase(x.c as any).execute({purchaseId:p.id,lineId:p.lines[0].id})).toMatchObject({removed:true})}],
["receive rejects empty receipt",async()=>{const x=ctx();const p=await po(x);await expect(uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[]})).rejects.toThrow("Receipt requires lines")}],
["receive rejects cancelled",async()=>{const x=ctx();const p=await po(x);await uc.cancelPurchaseUseCase(x.c as any).execute({id:p.id});await expect(uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]})).rejects.toThrow("Cancelled") }],
["unlinked receipt skips stock movement",async()=>{const x=ctx();const p=await po(x,{lines:[{titleSnapshot:"A",quantity:1,unitPurchaseCost:1}]});await uc.receivePurchaseUseCase(x.c as any).execute({purchaseId:p.id,idempotencyKey:"k",lines:[{purchaseLineId:p.lines[0].id,quantityReceived:1}]});expect(x.moves).toHaveLength(0)}],
];it.each(moreCases)("compatibility: %s",async(_n,run:any)=>run());
});
