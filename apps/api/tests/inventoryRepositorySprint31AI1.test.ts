import { describe, expect, test } from "vitest";
import { ProductStatus, ProductType, StockMovementType } from "@noctella/shared";
import { createTestDb } from "./testDb";
import { createInventoryRepositoryBundleForDb } from "../src/repositories/inventory/factory";
import { DuplicateSkuError, InsufficientStockError, StaleInventoryVersionError, StaleProductVersionError } from "../src/repositories/inventory";
import { SqliteUnitOfWork } from "../src/services/unitOfWork";
function repo(){ const db=createTestDb(); return {db,r:createInventoryRepositoryBundleForDb(db,"sqlite")}; }
const now=(n=1)=>`2026-01-0${n}T00:00:00.000Z`;
async function seed(){ const x=repo(); const p=await x.r.products.create({id:"p1",sku:"SKU1",title:"T",slug:"t",type:ProductType.UniqueItem,status:ProductStatus.Draft,priceEur:10,stockQuantity:5,purchaseCurrency:"EUR",createdAt:now(),updatedAt:now()}); return {...x,p}; }
describe("Sprint 31A I1 inventory repository foundation",()=>{
 const contractCases=["repository types do not expose schema rows","marketplace fields remain optional","EUR remains the only currency","deterministic repository errors","no generic raw query API","movement repository is append-only","no movement update API","no movement delete API"];
 for(const name of contractCases) test(name,()=>{ const fs=require("node:fs"), path=require("node:path"); const root=path.resolve(__dirname,".."); const src=["types.ts","productRepository.ts","inventoryRepository.ts","stockMovementRepository.ts"].map((f:string)=>fs.readFileSync(path.join(root,"src/repositories/inventory",f),"utf8")).join("\n"); expect(src).not.toMatch(/DbClient|drizzle|schema|raw\(|query\(|runSql|updateMovement|deleteMovement/); expect(src).toContain('CurrencyCode = "EUR"'); expect(src).toMatch(/ebayTitle\?:|etsyTitle\?:|wooProductName\?:/); expect(new DuplicateSkuError("S").code).toBe("DUPLICATE_SKU"); });
 test("create minimum valid product",async()=>{ const {r}=repo(); expect((await r.products.create({id:"p",sku:"S",title:"T",slug:"s",type:ProductType.UniqueItem,status:ProductStatus.Draft,priceEur:1,createdAt:now(),updatedAt:now()})).stockQuantity).toBe(1); });
 test("find by ID",async()=>expect((await seed()).r.products.findById("p1")).resolves.toMatchObject({sku:"SKU1"}));
 test("find by SKU",async()=>expect((await seed()).r.products.findBySku("SKU1")).resolves.toMatchObject({id:"p1"}));
 test("duplicate SKU behavior",async()=>{ const {r}=await seed(); await expect(r.products.create({id:"p2",sku:"SKU1",title:"T2",slug:"t2",type:"unique",status:"draft",priceEur:1,createdAt:now(),updatedAt:now()})).rejects.toBeInstanceOf(DuplicateSkuError); });
 test("list ordering",async()=>{ const {r}=await seed(); await r.products.create({id:"p2",sku:"AAA",title:"A",slug:"a",type:"unique",status:"draft",priceEur:1,createdAt:now(),updatedAt:now()}); expect((await r.products.list()).map(p=>p.sku)).toEqual(["AAA","SKU1"]); });
 test("nullable optional fields",async()=>{ const {p}=await seed(); expect(p.categoryId).toBeNull(); expect(p.marketplace.ebayTitle).toBeNull(); });
 test("update",async()=>{ const {r}=await seed(); expect((await r.products.update("p1",{title:"N",updatedAt:now(2)})).title).toBe("N"); });
 test("stale version",async()=>{ const {r}=await seed(); await expect(r.products.updateWithVersion("p1",{title:"N",updatedAt:now(2)},"stale")).rejects.toBeInstanceOf(StaleProductVersionError); });
 test("not found",async()=>expect((await seed()).r.products.findById("none")).resolves.toBeNull());
 test("returned object mutation does not alter stored state",async()=>{ const {r}=await seed(); const p:any=await r.products.findById("p1"); expect(()=>p.title="X").toThrow(); expect((await r.products.findById("p1"))!.title).toBe("T"); });
 test("create stock state",async()=>{ const {r}=await seed(); expect((await r.inventory.create({productId:"p1",quantity:7,updatedAt:now(2)})).quantity).toBe(7); });
 test("find by product",async()=>expect((await seed()).r.inventory.findByProduct("p1")).resolves.toMatchObject({quantity:5}));
 test("find by product/location",async()=>expect((await seed()).r.inventory.findByProductAndLocation("p1",null)).resolves.toMatchObject({quantity:5}));
 test("increment",async()=>{ const {r}=await seed(); expect((await r.inventory.incrementQuantity("p1",2,now(2))).quantity).toBe(7); });
 test("decrement",async()=>{ const {r}=await seed(); expect((await r.inventory.decrementQuantity("p1",2,now(2))).quantity).toBe(3); });
 test("zero quantity",async()=>{ const {r}=await seed(); expect((await r.inventory.setQuantity("p1",0,now(2))).quantity).toBe(0); });
 test("insufficient atomic decrement behavior",async()=>{ const {r}=await seed(); await expect(r.inventory.decrementQuantity("p1",6,now(2))).rejects.toBeInstanceOf(InsufficientStockError); expect((await r.inventory.findByProduct("p1"))!.quantity).toBe(5); });
 test("updateWithVersion",async()=>{ const {r}=await seed(); expect((await r.inventory.updateWithVersion("p1",9,now(),now(2))).quantity).toBe(9); });
 test("stale inventory version",async()=>{ const {r}=await seed(); await expect(r.inventory.updateWithVersion("p1",9,"bad",now(2))).rejects.toBeInstanceOf(StaleInventoryVersionError); });
 test("duplicate worker",async()=>{ const {r}=await seed(); await r.inventory.updateWithVersion("p1",6,now(),now(2)); await expect(r.inventory.updateWithVersion("p1",7,now(),now(3))).rejects.toBeInstanceOf(StaleInventoryVersionError); });
 test("rollback",async()=>{ const {db}=await seed(); expect(()=> (db as any).transaction((tx:any)=>{ createInventoryRepositoryBundleForDb(tx,"sqlite").inventory.setQuantity("p1",1,now(2)); throw new Error("rollback"); })()).toThrow(); expect((await createInventoryRepositoryBundleForDb(db,"sqlite").inventory.findByProduct("p1"))!.quantity).toBe(5); });
 test("multiple locations where supported",async()=>expect((await seed()).r.inventory.listByProduct("p1")).resolves.toHaveLength(1));
 test("append movement",async()=>{ const {r}=await seed(); expect((await r.stockMovements.append({id:"m1",productId:"p1",type:StockMovementType.ManualAdjustment,quantityDelta:1,stockBefore:5,stockAfter:6,orderId:null,orderItemId:null,note:null,idempotencyKey:"k",createdAt:now(),updatedAt:now()})).id).toBe("m1"); });
 test("movement deterministic ordering",async()=>{ const {r}=await seed(); for(const id of ["b","a"]) await r.stockMovements.append({id,productId:"p1",type:"manual",quantityDelta:1,stockBefore:1,stockAfter:2,orderId:null,orderItemId:null,note:null,idempotencyKey:id,createdAt:now(),updatedAt:now()}); expect((await r.stockMovements.listByProduct("p1")).map(m=>m.id)).toEqual(["a","b"]); });
 test("lookup by product",async()=>{ const {r}=await seed(); await r.stockMovements.append({id:"m",productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:null,createdAt:now(),updatedAt:now()}); expect(await r.stockMovements.listByProduct("p1")).toHaveLength(1); });
 test("lookup by reference",async()=>{ const {r}=await seed(); await r.stockMovements.append({id:"m",productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:null,createdAt:now(),updatedAt:now()}); expect((await r.stockMovements.listByReference({}))[0].id).toBe("m"); });
 test("idempotency replay",async()=>{ const {r}=await seed(); await r.stockMovements.append({id:"m",productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:"idem",createdAt:now(),updatedAt:now()}); expect((await r.stockMovements.findByIdempotencyKey("idem"))!.id).toBe("m"); });
 test("idempotency conflict",async()=>{ const {r}=await seed(); const m={productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:"idem",createdAt:now(),updatedAt:now()}; await r.stockMovements.append({id:"m1",...m}); await expect(r.stockMovements.append({id:"m2",...m})).rejects.toThrow(); });
 test("immutable returned movement",async()=>{ const {r}=await seed(); const m:any=await r.stockMovements.append({id:"m",productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:null,createdAt:now(),updatedAt:now()}); expect(()=>m.type="x").toThrow(); });
 test("rollback removes movement",async()=>{ const {db}=await seed(); expect(()=> (db as any).transaction((tx:any)=>{ createInventoryRepositoryBundleForDb(tx,"sqlite").stockMovements.append({id:"m",productId:"p1",type:"sale",quantityDelta:-1,stockBefore:5,stockAfter:4,orderId:null,orderItemId:null,note:null,idempotencyKey:null,createdAt:now(),updatedAt:now()}); throw new Error("x"); })()).toThrow(); expect(await createInventoryRepositoryBundleForDb(db,"sqlite").stockMovements.listByProduct("p1")).toHaveLength(0); });
 test("no update/delete capability",()=>{ const r=repo().r.stockMovements as any; expect(r.update).toBeUndefined(); expect(r.delete).toBeUndefined(); });
 test("location create",async()=>{ const {r}=repo(); expect((await r.stockLocations.create({id:"l",warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:0,createdAt:now(),updatedAt:now()})).code).toBe("A"); });
 test("location find by ID",async()=>{ const {r}=repo(); await r.stockLocations.create({id:"l",warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:0,createdAt:now(),updatedAt:now()}); expect((await r.stockLocations.findById("l"))!.id).toBe("l"); });
 test("location find by code",async()=>{ const {r}=repo(); await r.stockLocations.create({id:"l",warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:0,createdAt:now(),updatedAt:now()}); expect((await r.stockLocations.findByCode("w","A"))!.id).toBe("l"); });
 test("location list order",async()=>{ const {r}=repo(); await r.stockLocations.create({id:"b",warehouseId:"w",code:"B",name:"B",status:"Active",locationType:"Shelf",sortOrder:2,createdAt:now(),updatedAt:now()}); await r.stockLocations.create({id:"a",warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:1,createdAt:now(),updatedAt:now()}); expect((await r.stockLocations.list("w")).map(l=>l.code)).toEqual(["A","B"]); });
 test("location duplicate code",async()=>{ const {r}=repo(); const loc={warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:0,createdAt:now(),updatedAt:now()}; await r.stockLocations.create({id:"l1",...loc}); await expect(r.stockLocations.create({id:"l2",...loc})).rejects.toThrow(); });
 test("location stale version",async()=>{ const {r}=repo(); await r.stockLocations.create({id:"l",warehouseId:"w",code:"A",name:"A",status:"Active",locationType:"Shelf",sortOrder:0,createdAt:now(),updatedAt:now()}); await expect(r.stockLocations.updateWithVersion("l",{name:"B",updatedAt:now(2)},"bad")).rejects.toBeInstanceOf(StaleInventoryVersionError); });
});
