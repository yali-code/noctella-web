export class RefundProviderBoundaryError extends Error { constructor(public readonly code: string, message: string, public readonly retryable = false) { super(message); this.name = "RefundProviderBoundaryError"; } }
export const unsupportedRefundProvider = (kind: "marketplace"|"payment", providerId: string) => new RefundProviderBoundaryError("UNSUPPORTED_PROVIDER", `Unsupported ${kind} refund provider: ${providerId}`, false);
export const duplicateRefundProvider = (kind: "marketplace"|"payment", providerId: string) => new RefundProviderBoundaryError("DUPLICATE_PROVIDER", `Duplicate ${kind} refund provider: ${providerId}`, false);
export const unsupportedRefundCapability = (operation: string, providerId: string) => new RefundProviderBoundaryError("UNSUPPORTED_PROVIDER_CAPABILITY", `Refund provider ${providerId} does not support ${operation}`, false);
export function normalizeRefundProviderError(error: unknown) { if (error instanceof RefundProviderBoundaryError) return { outcome: error.retryable ? "retryable_failure" as const : "terminal_failure" as const, code: error.code, message: error.message }; const e = error as { code?: string; type?: string; message?: string; retryable?: boolean }; return { outcome: e?.retryable ? "retryable_failure" as const : "terminal_failure" as const, code: e?.code ?? e?.type ?? "PROVIDER_ERROR", message: String(e?.message ?? "Refund provider failed") }; }
