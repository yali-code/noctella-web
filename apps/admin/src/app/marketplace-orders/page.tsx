export const dynamic = "force-dynamic";
import Link from "next/link";
import { listMarketplaceOrders, retryEligible } from "../../lib/marketplaceSync";
export default async function MarketplaceOrdersPage({ searchParams }: { searchParams: Record<string,string> }) { const qs=new URLSearchParams(searchParams).toString(); const data=await listMarketplaceOrders(qs?`?${qs}`:""); return <main><h1>Marketplace Orders</h1><p>Searchable imported eBay and Etsy orders with retry visibility.</p><table><thead><tr><th>Channel</th><th>External order</th><th>Buyer</th><th>Total</th><th>Status</th><th>Internal</th><th>Action</th></tr></thead><tbody>{data.items.map((o:any)=><tr key={o.id}><td>{o.channel}</td><td><Link href={`/marketplace-orders/${o.id}`}>{o.externalOrderNumber??o.externalOrderId}</Link></td><td>{o.buyerName??o.buyerEmail??"—"}</td><td>{o.total} {o.currency}</td><td>{o.status}</td><td>{o.internalOrderId?<Link href={`/orders/${o.internalOrderId}`}>{o.internalOrderId}</Link>:"—"}</td><td>{retryEligible(o)?"Retry available":"—"}</td></tr>)}</tbody></table></main>; }
