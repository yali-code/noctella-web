import { getErpProductWorkspace, landedCostCompleteness, mapAvailability, mapPublishReadiness, productPhotosLink, workflowLabel } from "../../../../lib/erpInventoryBridge";
export default async function ProductWorkspacePage({ params }: { params: { id: string } }) { const w:any = await getErpProductWorkspace(params.id); const availability = mapAvailability(w); return <main><h1>Product Workspace</h1><section><h2>ERP identity</h2><p>ERP reference: {w.erpReferenceId ?? "—"}</p><p>Noctella ID: {w.noctellaId ?? "—"}</p><p>Barcode: {w.barcodeValue ?? "—"}</p><p>Location: {w.storageLocationReference ?? "—"}</p></section><section><h2>Operational metadata</h2><p>Workflow: {workflowLabel(w.workflowStatus)}</p><p>Photos: {workflowLabel(w.photoStatus)} — <a href={productPhotosLink(params.id)}>Manage photos</a></p><p>Authentication: {workflowLabel(w.authenticationStatus)}</p><p>Marketplace: {workflowLabel(w.marketplacePreparationStatus)}</p></section><section><h2>Landed cost</h2><p>{landedCostCompleteness(w.landedCost)} / EUR {w.landedCost?.landedCost ?? "unknown"}</p><p>Expected profit: {w.landedCost?.expectedGrossProfit ?? "unknown"}</p><p>Expected ROI: {w.landedCost?.expectedRoi ?? "unknown"}</p></section><section><h2>Availability</h2><p>{availability.label} ({availability.physical} physical, {availability.reserved} reserved)</p></section><section><h2>Readiness</h2><p>{mapPublishReadiness(w.publishReadinessSummary)}</p></section></main>; }
